# Core
streamlit==1.38.0
pandas==2.2.2
requests==2.32.3

# Resolve known auth/core compatibility (used by some Streamlit deps)
# Pin to mutually compatible versions to avoid import/runtime conflicts
google-auth==2.31.0
google-api-core==2.19.1
